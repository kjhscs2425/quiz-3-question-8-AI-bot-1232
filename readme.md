# Instructions  

The file `expenses.json` contains a dictionary. The keys of the dictionary are the stores that I visited. The values are lists of things I bought. Each thing I bought is a dictionary with two keys ("item" and "price").

1. Read the dictionary from `expenses.json`.
2. Add up to total price for all the expenses at the "pet store".
3. Print out the total price. (Should be `115`)